# Database Configuration (use env vars on Render)
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://dpg-d48ldvfdiees73a0tvig-a.oregon-postgres.render.com/busbookingdb_90he}
spring.datasource.username=${DB_USERNAME:rwuser}
spring.datasource.password=${DB_PASSWORD:raRGJysQvIBrg9XlV92gZc1UaJrcctpv}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration (use env var in production)
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=604800000

# Server Configuration
# keep default or let Docker ENTRYPOINT override with -Dserver.port=${PORT}
server.port=${PORT:8080}
spring.application.name=jwt-auth-backend

# CORS Configuration
# add your deployed frontend origin so backend responds with Access-Control-Allow-Origin
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://awd-22127294-ia04-fe.onrender.com,http://localhost:3000,http://localhost:5173}

# Improve error handling to avoid Tomcat failing while rendering the HTML error page
server.error.whitelabel.enabled=false
server.error.include-message=always
server.error.include-stacktrace=on_trace_param
server.error.include-binding-errors=always

# Prevent MVC from favoring path-extension based view resolution which can cause view lookup errors for /error
spring.mvc.contentnegotiation.favor-path-extension=false
spring.mvc.contentnegotiation.favor-parameter=false

# Ensure no unexpected exception is thrown when no handler is found (let BasicErrorController handle it)
spring.mvc.throw-exception-if-no-handler-found=false