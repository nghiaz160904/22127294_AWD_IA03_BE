# Database Configuration (use env vars on Render)
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://dpg-d48ldvfdiees73a0tvig-a.oregon-postgres.render.com/busbookingdb_90he}
spring.datasource.username=${DB_USERNAME:rwuser}
spring.datasource.password=${DB_PASSWORD:raRGJysQvIBrg9XlV92gZc1UaJrcctpv}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration (use env var in production)
jwt.secret=${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=604800000

# Server Configuration
# keep default or let Docker ENTRYPOINT override with -Dserver.port=${PORT}
server.port=${PORT:8080}
spring.application.name=jwt-auth-backend

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}